{% extends "base.html" %}
{% block content %}
<section class="section narrow">
    <h1>Blog</h1>
    {% for post in posts %}
    <article class="card blog-card">
        <h2>{{ post.title }}</h2>
        <span class="meta">{{ post.date }}</span>
        <p>{{ post.summary }}</p>
    </article>
    {% endfor %}
</section>
{% endblock %}
