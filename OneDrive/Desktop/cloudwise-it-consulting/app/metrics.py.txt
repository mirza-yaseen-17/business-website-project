from prometheus_client import Counter, Histogram

REQUEST_COUNT = Counter(
    "cloudwise_request_count",
    "Total HTTP requests",
    ["method", "endpoint", "http_status"]
)

REQUEST_LATENCY = Histogram(
    "cloudwise_request_latency_seconds",
    "Request latency",
    ["endpoint"]
)
