apiVersion: v1
kind: Service
metadata:
  name: cloudwise-web-svc
spec:
  type: {{ .Values.service.type }}
  selector:
    app: cloudwise-web
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 5000
      protocol: TCP
      name: http
